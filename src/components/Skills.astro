---
import Icon from "./Icon.astro";
---

<section class="box skills">
	<div class="stack gap-2 lg:gap-4">
		<Icon
			icon="code"
			color="var(--accent-regular)"
			size="2.5rem"
			gradient
		/>
		<h2>Create quality projects</h2>
		<p>Have the opportunity to work on interesting projects.</p>
	</div>
	<div class="stack gap-2 lg:gap-4">
		<Icon
			icon="trophy"
			color="var(--accent-regular)"
			size="2.5rem"
			gradient
		/>
		<h2>Upgrade technical skills</h2>
		<p>
			Improve technical skills through collaboration on projects and
			learning from other community members.
		</p>
	</div>
	<div class="stack gap-2 lg:gap-4">
		<Icon
			icon="briefcase"
			color="var(--accent-regular)"
			size="2.5rem"
			gradient
		/>
		<h2>Boost portfolios</h2>
		<p>
			Add compelling projects to your portfolio to attract future
			employers or clients.
		</p>
	</div>
</section>

<style>
	.box {
		border: 1px solid var(--gray-800);
		border-radius: 0.75rem;
		padding: 1.5rem;
		background-color: var(--gray-999_40);
		box-shadow: var(--shadow-sm);
	}

	.skills {
		display: flex;
		flex-direction: column;
		gap: 3rem;
	}

	.skills h2 {
		font-size: var(--text-lg);
	}

	.skills p {
		color: var(--gray-400);
	}

	@media (min-width: 50em) {
		.box {
			border-radius: 1.5rem;
			padding: 2.5rem;
		}

		.skills {
			display: grid;
			grid-template-columns: repeat(3, 1fr);
			gap: 5rem;
		}

		.skills h2 {
			font-size: var(--text-2xl);
		}
	}
</style>
